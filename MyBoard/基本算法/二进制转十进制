int binToDec(string s) {
    int res = 0;
    for (int i = 0; i < s.size(); i++) {
        res = res * 2 + (s[i] - '0');
    }
    return res;
}

// C++ 中将整数转为固定长度二进制字符串的经典技巧
//将i转化为n位二进制数;
string s=bitset<32>(i).to_string().substr(32 - n);