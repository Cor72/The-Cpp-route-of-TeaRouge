{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build C project",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",                    // 生成调试信息
                "${file}",               // 当前打开的C文件
                "-o",                    // 输出
                "${fileDirname}\\${fileBasenameNoExtension}.exe", // 输出路径
                "-std=c11"               // C标准
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Generated task by VS Code"
        }
    ]
}
